---
# ansible-galaxy collection install -r requirements.yml
# pip install pan-os-python      # required by the Palo Alto modules
collections:
  - name: arista.eos
    version: ">=10.0.0"
  - name: cisco.ios
    version: ">=6.0.0"
  - name: paloaltonetworks.panos
    version: ">=2.20.0"
  - name: ansible.netcommon
    version: ">=6.0.0"
