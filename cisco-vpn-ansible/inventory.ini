[branch_routers]
br01 ansible_host=10.1.5.145

[branch_routers:vars]
ansible_network_os=cisco.ios.ios
ansible_connection=network_cli
ansible_user=admin
# Keep these in vault, not here. See group_vars/branch_routers/vault.yml
# ansible_password and ansible_become_password are pulled from vault.
ansible_become=yes
ansible_become_method=enable
