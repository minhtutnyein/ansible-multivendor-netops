[firewalls]
pa-vm01 ansible_host=10.1.5.5

[firewalls:vars]
# PAN-OS modules call the firewall XML API directly from the control node,
# so the Ansible connection is local (NOT ssh/network_cli).
ansible_connection=local
ansible_python_interpreter={{ ansible_playbook_python }}
